export enum SPEED_UNIT {
  KMH = "km/h",
  MPH = "mph",
  MPS = "m/s", // default
}

export enum DISTANCE_UNIT {
  KM = "km",
  M = "m", // default
  CM = "cm",
  MM = "mm",
  INCH = "inch",
  FT = "ft",
  YD = "yd",
  MILE = "mile",
}

export enum TIME_UNIT {
  S = "s", // default
  MIN = "min",
  H = "h",
}

export enum ACCELERATION_UNIT {
  MPS2 = "m/s^2", // default
}

export enum TEMPERATURE_UNIT {
  C = "C", // default
  F = "F",
}

export enum ANGLE_UNIT {
  DEG = "deg",
  RAD = "rad", // default
}
